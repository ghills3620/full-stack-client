{{#each wods as |wod|}}
  {{log wod}}
  <!-- {{> partial}}
  Don't uncomment yet -->

  <div>
    <h3 data-id="{{wod.id}}"> <small>Athelete: {{wod.user.email}}</small></h3>
    <section data-id="{{wod.id}}">
    <p>{{#if wod.id}}
      <medium>Metcon </medium>{{wod.metcon}}  <medium>Results </medium>{{wod.result}}
    {{else}}
      go workout.
    {{/if}}
  </p>
  </section>
    <button type="submit" class="btn btn-default">Remove</button>
    <button type="submit" class="btn btn-default">Update</button>
  </section>
  </div>
  <br>

{{/each}}
